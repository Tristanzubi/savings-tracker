# Prisma Postgres Database URL
# 1. Create a free account at https://console.prisma.io
# 2. Create a new Prisma Postgres database
# 3. Copy your DATABASE_URL connection string and paste it below
# 4. Run: npx prisma db pull (to sync schema from database if it exists)
# 5. Run: npx prisma generate (to generate Prisma Client)

DATABASE_URL="postgresql://user:password@host:port/dbname"

# Better Auth Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your_generated_secret_here"
BETTER_AUTH_URL="http://localhost:3000"
